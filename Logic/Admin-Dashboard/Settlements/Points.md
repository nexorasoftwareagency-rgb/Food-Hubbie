# Points / Gotchas: SettlementsPage

- **No Firebase integration** — `MOCK_TRANSACTIONS` constant
- **PDF export is mislabeled** — actually generates CSV, not PDF
- **No date filtering or search**
- **No real settlement processing** — view-only display
- **Status badges**: "settled" = green, "pending" = orange
