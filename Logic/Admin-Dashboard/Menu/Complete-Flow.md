## Complete-Flow.md

Flow: open Menu → view dish grid → search/filter → click Add or Edit → fill form (name, category, price, sizes, stock, threshold, addons) → Save → Firebase update → grid re-renders → auto-migration silently fixes old boolean stock
