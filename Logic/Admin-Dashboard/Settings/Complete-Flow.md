## Complete-Flow.md

Flow: open Settings → three tabs → Store tab: edit fields → Save → validates coords/GSTIN/FSSAI → writes to Firebase → Delivery tab: edit phones/backup code → manage slabs (add/update/remove) → Save → writes to Firebase → Display tab: placeholder
