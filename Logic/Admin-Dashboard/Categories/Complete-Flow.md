## Complete-Flow.md

Flow: open Categories → view list with images → click "Add Category" → fill name, image URL, order → Save → Firebase push → list updates → click Delete → remove from Firebase → list updates
