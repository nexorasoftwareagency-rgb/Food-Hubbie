## Points.md

- No edit feature — user must delete and recreate to change a category
- Image is URL string (not uploaded file) — no image upload in this component
- Serial number shown is array index, not stored field — reordering changes display
- Addons on category level can be inherited by POS modal (see POSPage `catAddons`)
