## Complete-Flow.md

Flow: open Customers → Firebase loads customers + orders → client-side aggregation matches by phone → computes order count + LTV → sorts by LTV descending → renders table with WhatsApp links → Export CSV
