# Search Page — Database Structure

Uses same paths as Home:
- `businesses/{bizId}/outlets/{outletId}` — outlet names, cuisine, tags
- `businesses/{bizId}/outlets/{outletId}/dishes/{dishId}` — dish names, categories

No additional database paths required.
