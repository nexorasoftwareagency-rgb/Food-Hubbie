# Outlets Page — Points

- All outlets loaded at once — performance concern if 100+ outlets
- No server-side search — text filter is client-side `.includes()`
- `isVegOnly` on outlet means the entire outlet is veg; individual item veg status not checked
