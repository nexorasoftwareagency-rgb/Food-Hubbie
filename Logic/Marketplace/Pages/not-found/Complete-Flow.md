# not-found Page — Supporting Files

## Firebase
No Firebase interaction — purely static UI.

## Points
- Wouter's `<Switch>` renders this as the last `<Route>` without a `path` prop
- No analytics tracking for 404s
- Simple static component with "Go Home" link
