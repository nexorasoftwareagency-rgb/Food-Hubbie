# Archive

Legacy code kept for historical reference only. **Do not deploy or build from this directory.**

- `Admin-Previous-legacy-v1/` — the vanilla-JS ShopAdmin that preceded the React-based `admin-dashboard` v2. See `docs/shopadmin-v2-parity.md` for the migration map.
- `bot-reference-dummy/` — a duplicate of the WhatsApp bot project that was previously at the repo root as `Dummy Bot Data to Study the Logic/`. Study reference only; the canonical bot lives at `bot/`.
